<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dfrz.javaprojectstage3.mapper.PermissionMapper">

    <select id="getPermissionsByRoleId" resultType="Permission">
         select d.id,d.permissionName title,d.pid,d.permissionKey  from  t_role_permission c LEFT JOIN t_permissions d on c.permission_id=d.id where c.role_id=#{role_id}
    </select>

    <!--判断角色是否有权限-->
    <select id="getRolePermissionCount" resultType="int">
        select count(*) from t_role_permission
        where role_id=#{role_id} and permission_id=#{permission_id}
    </select>

</mapper>